# Session cookie encryption — set a random 32+ char secret in production
NUXT_SESSION_PASSWORD=change-me-to-a-random-32-char-secret!!

# Keyed hash pepper for passcodes — NEVER change after go-live (it voids every passcode)
NUXT_PASSCODE_PEPPER=change-me-too

# Token the host cron sends to /api/cron/tick
NUXT_CRON_TOKEN=change-me-cron

# Web push (generate once: npx web-push generate-vapid-keys)
NUXT_PUBLIC_VAPID_PUBLIC_KEY=
NUXT_VAPID_PRIVATE_KEY=
NUXT_VAPID_SUBJECT=mailto:you@example.org

# Optional email for parent announcements (https://resend.com — free tier)
NUXT_RESEND_API_KEY=
NUXT_EMAIL_FROM=Scout Passport <passport@yourdomain.org>

# Optional SMS via SMS.to (https://sms.to) — used for announcements to anyone
# with a phone on file, and for texting/re-texting a member their passcode.
# 1. Sign up at sms.to, then Settings -> API Keys -> create a key -> paste below.
# 2. Top up credit — SMS.to is pay-as-you-go, no monthly fee.
# 3. NUXT_SMS_SENDER_ID is the "from" name shown to recipients. Leave it blank
#    to use SMS.to's shared default number — that always delivers.
#    A custom sender ID must be 1-11 ALPHANUMERIC characters with NO SPACES
#    (e.g. "Scouts30", not "Scouts 30"), AND registered/approved first in the
#    SMS.to dashboard under Sender IDs — required in Cyprus/EU. An unregistered
#    or malformed sender ID is a common cause of "the API accepted it but the
#    SMS never arrived". Anything invalid here is ignored with a warning in the
#    logs and the message is sent from the shared number instead.
NUXT_SMS_TO_API_KEY=
NUXT_SMS_SENDER_ID=

# SQLite location (the docker volume mounts /data)
NUXT_DB_PATH=./data/passport.db
